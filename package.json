{
  "name": "sequence-query-builder",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Static builder for FIQL/RSQL query strings over a sequence database",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "tsc --noEmit"
  },
  "license": "MIT",
  "devDependencies": {
    "jsdom": "^30.1.0",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
